# 照片水印工具 · 使用说明

> 适用版本：semi-utils-win.zip（Windows 独立版，无需安装 Python）

---

## 一、系统要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 / 11（64 位） |
| 磁盘空间 | 解压后约 200 MB |
| 网络 | 不需要 |
| 其他 | 无需安装 Python、无需安装任何依赖 |

---

## 二、解压安装

1. 将收到的 `semi-utils-win.zip` **解压到任意文件夹**，例如 `D:\semi-utils-win\`

2. 解压后的目录结构如下：

```
semi-utils-win\
├── semi-utils.exe      ← 主程序
├── start.bat           ← 启动脚本（双击这个）
├── config\             ← 配置文件（字体、Logo、水印模板）
└── exiftool\           ← EXIF 读取工具（自动调用，无需操作）
```

> ⚠️ **请勿将程序放在中文路径或含空格的路径下**，例如 `D:\我的文件\` 可能导致异常。  
> 推荐放在 `C:\` 或 `D:\` 根目录下的纯英文文件夹内。

---

## 三、使用步骤

### 第一步：启动程序

**双击 `start.bat`**，会自动完成以下操作：

- 在同目录下创建 `input\` 和 `output\` 文件夹（首次运行时自动创建）
- 启动主程序，自动打开浏览器访问操作界面

> 如果浏览器没有自动打开，请手动在浏览器中访问：**http://localhost:15050**

启动后会出现一个黑色命令行窗口，**运行期间请不要关闭它**，关闭即退出程序。

---

### 第二步：放入照片

将需要处理的照片复制到 **`input\`** 文件夹中。

**支持的格式：**

| 格式 | 说明 |
|------|------|
| `.jpg` / `.jpeg` | 常见相机 / 手机照片 |
| `.png` | PNG 图片 |
| `.heic` | iPhone 拍摄的 HEIC 格式 |
| `.tiff` | 高质量 TIFF 格式 |
| `.webp` | WebP 格式 |

---

### 第三步：在浏览器中处理

1. 打开浏览器页面（地址：http://localhost:15050）
2. 点击右上角的 **刷新** 按钮，让文件列表加载 `input\` 里的照片
3. 勾选需要处理的文件（默认全选）
4. 点击 **开始处理** 按钮
5. 等待进度完成，右下角会显示成功/失败数量

---

### 第四步：取走成品

处理完成的照片会自动保存到 **`output\`** 文件夹，文件名与原图相同。

---

## 四、水印内容说明

程序会自动读取照片的 EXIF 信息，在照片底部生成白色水印区域，显示以下内容：

| 位置 | 内容 |
|------|------|
| 左侧 | GPS 经纬度（如无 GPS 信息则为空） |
| 右侧 | 拍摄时间 |

> 照片必须包含 EXIF 信息才能读取上述数据。  
> 使用微信/QQ 等压缩后转发的照片通常已丢失 EXIF，建议使用原图。

---

## 五、常见问题

### Q1：双击 `start.bat` 窗口一闪而过，程序没有启动？

- 检查解压路径是否含有**中文或空格**，如有请移到纯英文路径
- 检查 `input\` 和 `output\` 文件夹是否存在，不存在则手动创建

---

### Q2：浏览器打开后，刷新文件列表显示为空？

- 确认照片已复制到 `input\` 文件夹（不是放到了其他地方）
- 点击文件列表右侧的 **刷新图标** 重新加载

---

### Q3：处理完成但照片显示"失败"？

- 照片格式不在支持列表内
- 照片文件损坏或被其他程序占用
- 照片路径含有特殊字符

---

### Q4：output 里的照片水印区域是空白的（没有经纬度和时间）？

照片没有 EXIF 信息。请使用相机原图，不要使用经过微信/截图等方式传输的照片。

---

### Q5：杀毒软件提示 `semi-utils.exe` 有风险？

这是 Python 程序打包成 exe 后的常见误报（PyInstaller 打包机制触发启发式检测）。  
程序本身不含任何恶意代码，将程序所在文件夹添加到杀毒软件白名单即可正常使用。

---

### Q6：如何退出程序？

关闭黑色命令行窗口（或在窗口中按 `Ctrl+C`）即可退出。  
关闭浏览器标签页不会停止程序。

---

## 六、目录结构速查

```
semi-utils-win\
├── start.bat           ← 每次使用前双击此文件启动
├── semi-utils.exe      ← 主程序（勿删除）
├── config\             ← 配置（勿删除）
│   ├── fonts\          ← 字体文件
│   ├── logos\          ← 相机品牌 Logo
│   └── templates\      ← 水印样式模板
├── exiftool\           ← EXIF 工具（勿删除）
├── input\              ← 【放入原始照片】（首次启动后自动创建）
└── output\             ← 【取走处理后的照片】（首次启动后自动创建）
```

---

如遇到以上未列出的问题，请将命令行窗口中的**错误信息截图**发给技术支持。
